# 프로젝트: 시니어 건강 숏츠 자동 생성

## 목적
노인 시청자를 위한 40~50초 건강 지식 유튜브 숏츠를
주제 입력 → 영상 파일까지 자동 생성한다.

## 파이프라인
topics.csv 한 행 → 01 대본 → 02 TTS → 03 자막 타이밍 → 04 배경 소재 → 05 렌더 → output/{id}.mp4
06 업로드는 별도 명령으로만 실행하고, 이 환경에서는 실행하지 않는다.

## 대본 규칙
- 한 문장 15자 내외, 한 영상에 핵심 메시지 1개
- 첫 문장은 결론("이것 하나만 기억하세요")
- 전문용어 금지, 존댓말, 어르신 호칭
- 마지막 문장은 핵심 메시지 반복
- sources/의 근거만 사용. 특정 약, 건강기능식품, 치료법 권장 금지
- "병원에 가세요" 같은 안전 문구를 자연스럽게 포함

## 영상 규칙
- 1080x1920, 30fps, 자막 흰색+검은 테두리 90px 이상, 한 줄 10자
- 컷 전환 최소 3초, 배경음악 -20dB 이하
- 나레이션 속도 0.85x

## 보안
- 키는 환경변수에서만 읽는다. 파일에 쓰지 않는다.
- token.json, .env, output/ 은 절대 커밋하지 않는다.
- 세션 종료 시 output/ 을 비운다.

## 작업 방식
- 스크립트를 수정하면 topics.csv 첫 행으로 1편 테스트 후 보고
- 외부 API 호출 비용이 드는 작업은 실행 전에 확인 요청
